{% extends 'base.html.twig' %}

{% block title %}Add Voiture{% endblock %}

{% block body %}
    <div class="page-container">
        <br>
        <br>
        <br>
        
        <h1 class="table-title">Add a New Voiture</h1>

        <a href="{{ path('app_client_index') }}" class="back-button">Retour à la liste</a>

        <div class="form-container">

        {{ form_start(form) }}
            {{ form_row(form.libelle) }}
            {{ form_row(form.pays) }}
            <button type="submit" class="btn btn-success">Save</button>
        {{ form_end(form) }}
        </div>
    </div>
{% endblock %}




