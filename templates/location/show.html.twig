{% extends 'base.html.twig' %}

{% block title %}Location{% endblock %}

{% block body %}
   <br>
    <br>
    <br>
    <h1 class="table-title">Détails du location</h1>

        <a href="{{ path('app_location_index') }}" class="back-button">back to list</a>
        <a href="{{ path('app_location_edit', {'id': location.id}) }}" class="edit-button"> edit </a>
        {{ include('location/_delete_form.html.twig') }}

        <div class="table-container">

            <table class="styled-table">
                <tbody>
                    <tr>
                        <th>Id</th>
                        <td>{{ location.id }}</td>
                    </tr>
                    <tr>
                        <th>DateD</th>
                        <td>{{ location.dateD ? location.dateD|date('Y-m-d') : '' }}</td>
                    </tr>
                    <tr>
                        <th>DateA</th>
                        <td>{{ location.dateA ? location.dateA|date('Y-m-d') : '' }}</td>
                    </tr>
                    <tr>
                        <th>Prix</th>
                        <td>{{ location.prix }}</td>
                    </tr>
                </tbody>
            </table>
        </div>
   

{% endblock %}
